import { NgModule } from '@angular/core';
import { DataService } from '../services/data'; // Adjust the path as needed

@NgModule({
  providers: [DataService] // Add your services here
})
export class ServiceModule {}
